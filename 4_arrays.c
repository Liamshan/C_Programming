 #include <stdio.h>
 #include <stdlib.h>


int main() {
    int luckyNumbers[] = {4, 8, 15, 16, 23}; 
    int luckyBoys[20];  //If you don't assign values up front, then you need to declare the size. 

    printf("%d", luckyNumbers[0]);   //print index position 0

    return 0;
}
